# frozen_string_literal: true

source "https://rubygems.org"

group :development do
  gem "bundler", "~> 1.12"
  gem "rake", ">= 10.0"

  # [`rake-compiler`](https://github.com/rake-compiler/rake-compiler). Simply here to compile inline; not needed for the
  # gem to install itself properly.
  gem "rake-compiler", "~> 1.0"

  # [`rubocop`](https://github.com/bbatsov/rubocop).
  gem "rubocop", "~> 0.43.0"
end

group :doc do
  gem "redcarpet"

  # [`yard`](https://github.com/lsegal/yard).
  gem "yard", "~> 0.9.5"
end

gemspec
